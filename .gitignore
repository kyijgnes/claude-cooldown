__pycache__/
*.pyc
.env*
node_modules/

# 내부 개발 메모 — 공개 저장소(친구 배포용)에 올리지 않는다. 친구용 README.md 만 공개.
CLAUDE.md
server/README.md

# PyInstaller 산출물 — 40MB 짜리라 저장소에 넣지 않는다.
# 만드는 법은 build_exe.py, 나눠주는 법은 README 의 '친구에게 나눠주기' 참고.
build/
dist/
*.spec
pc/windows/앱아이콘.ico

# Next.js 릴레이 서버 (server/)
.next/
next-env.d.ts

# 안드로이드 (android/)
.gradle/
android/local.properties
android/.kotlin/
*.apk
*.aab

# 릴리스 서명 키 — 절대 올리지 말 것. 백업은 GitHub Secret 쪽에 있다(CLAUDE.md 참고).
android/keystore/
android/keystore.properties
