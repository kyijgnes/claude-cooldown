# CLAUDE.md

**클로드 쿨다운** — Claude 구독 한도(5시간 / 주간)를 **바탕화면 위젯**과
**갤럭시 폰 앱**(상태바·홈 위젯·잠금화면·AOD·라이브 배경화면)으로 보여주는 개인 도구.
친구들에게도 배포할 예정.

- repo: `claude-cooldown` / 엔드포인트: `/api/cooldown`
- 파이썬 모듈·파일은 `cooldown_` 접두사, 로컬 설정 파일은 `~/.claude_cooldown*.json`

## 구조

**윈도우 앱**(주력)은 서버를 거치지 않고 로컬에서 직접 조회한다.
**폰 앱**은 서버 릴레이로 받는다 (Vercel 에 배포돼 있다 — `server/README.md`).

```
내 PC (OAuth 토큰은 여기서만 — 절대 안 나간다)
  └─ pc/windows/cooldown_app.py   바탕화면 위젯 + 트레이. 직접 조회
        │  조회 성공할 때마다 POST {key, 퍼센트·초기화시각}   (pc/cooldown_push.py)
        ▼
  server/  Next.js API route → Upstash Redis (REST)
        ▲
        │  GET ?key=...   15분마다
  android/  갤럭시 앱 — 상태바 알림 · 홈 위젯 · 라이브 배경화면
```

- `key` = 랜덤 32 hex. 읽기 비밀번호 역할. PC 위젯 우클릭 > **폰에서 보기 > 폰 연결…**
  에서 QR(`claudecooldown://pair?url=..&key=..`)로 폰에 넘긴다.
- **PC 가 꺼져 있어도 폰 화면은 안 틀린다** — 퍼센트는 클로드를 쓸 때만 변하고,
  초기화는 폰이 `resets_at` 을 보고 스스로 0% 로 맞춘다(`Limit.settled`).

## 파일

| 경로 | 역할 |
|---|---|
| `pc/cooldown_core.py` | **조회·파싱 공용 모듈. 파서·속도 분석(`pace`)은 여기 한 곳에만 둔다** |
| `pc/cooldown_ping.py` | **자동 핑(모닝 스타터) 순수 로직.** 설정·전송·"지금 쏠까" 판단. Tk 없음 |
| `pc/cooldown_stats.py` | **사용량 기록·통계 순수 로직.** 한 줄씩 쌓기(`record`)·셈(`analyze`). Tk 없음 |
| `pc/windows/cooldown_app.py` | **윈도우 앱 본체.** 창·트레이·조회·메뉴. 그리기는 안 한다 |
| `pc/windows/skins/base.py` | 스킨이 지켜야 할 약속(Skin) + 공용 색·글꼴 |
| `pc/windows/skins/{card,arc,table,slim}.py` | 디자인 4종. 우클릭 > 디자인 으로 전환 |
| `pc/windows/_shot_skin.py` | 스킨을 4가지 상태로 렌더해 PNG 로 남기는 개발 도구 |
| `pc/windows/클로드 쿨다운 실행.bat` | 콘솔 없이 앱을 띄우는 실행 파일 |
| `pc/cooldown_push.py` | **폰으로 보내기.** 키 생성·주소 정돈·POST·QR 만들기 |
| `pc/agent/cooldown_agent.py` | 헤드리스 전용 상주 에이전트(위젯 안 쓰는 PC용). 보통은 필요 없다 |
| `server/app/api/cooldown/route.ts` | POST(업서트) / GET(조회) 릴레이 |
| `server/README.md` | **릴레이 문서 한 곳.** 저장소 만들기·환경변수·확인·되돌리기·무료 한도 계산 |
| `server/relay_check.mjs` | 릴레이가 폰과 맺은 약속을 지키는지 확인(저장소를 또 바꿀 때의 합격 기준) |
| `android/` | **갤럭시 앱** — 아래 '폰 앱' 절 |
| `android/art/make_claudi_icon.py` | **앱 아이콘을 데스크탑 도트 표에서 뽑는다.** 마스코트를 고치면 이걸 다시 돌린다 |
| `android/.../Look.kt` | **배경화면 꾸미기 값 한 덩어리(`Values`).** 읽기·쓰기도 여기만 |
| `android/.../WallpaperGrab.kt` | 폰에 걸려 있던 배경화면을 한 장 떠 온다(기본 배경) |
| `.github/workflows/android.yml` | 태그 밀면 APK 만들어 릴리스에 붙임 |
| `pc/requirements.txt` | requests / pillow / pystray / pywin32 / qrcode |
| `README.md` | 설치·배포 절차 |

## 확정된 사실

- 사용량 조회: `GET https://api.anthropic.com/api/oauth/usage`
  - 헤더: `Authorization: Bearer <token>`, `anthropic-beta: oauth-2025-04-20`
  - 토큰 위치: `~/.claude/.credentials.json` → `claudeAiOauth.accessToken`
  - **공식 문서화되지 않은 엔드포인트.** 예고 없이 바뀔 수 있음.
    공식 API 요청 이슈: anthropics/claude-code#13585
- 폴링 간격 300초 고정. 더 짧으면 rate limit 에 걸린다.
- Claude Code 2.1.x 이상은 statusline 에 `rate_limits.five_hour` / `.seven_day` 를
  stdin 으로 넘겨준다. 네트워크 없이 쓰는 대안 경로 (statusline 용으로만 유효).
- KWGT 는 `$wg(url, json, .path)$` 로 JSON 파싱 가능. Flow 의 Time 트리거로
  주기 제어 가능. 둘 다 Pro 기능.

## 응답 형식 (2026-07-28 실측 확인)

```json
{"five_hour": {"utilization": 7.0, "resets_at": "2026-07-27T22:19:59+00:00"},
 "seven_day": {"utilization": 55.0, "resets_at": "..."},
 "seven_day_opus": null, "seven_day_sonnet": null,
 "limits": [{"kind": "weekly_scoped", "percent": 7,
             "scope": {"model": {"display_name": "Fable"}}}, ...],
 "extra_usage": {...}, "spend": {...}}
```

- `utilization` 은 **0~100 스케일**이다. 100 을 곱하지 말 것.
- 모델별 주간 한도는 `seven_day_opus` 같은 최상위 필드가 아니라 **`limits[]` 의
  `kind == "weekly_scoped"`** 에 들어온다. 최상위 필드는 이제 항상 null.
- 형식이 또 바뀌면 고칠 곳은 `pc/cooldown_core.py` 한 곳뿐.
  확인은 `python pc/cooldown_core.py` (원본 JSON 출력).

## 이번 주 사용 속도 (`cooldown_core.pace`)

"지금쯤 얼마나 썼어야 하나"를 견줘 이번 주 페이스가 적당한지 보여 준다.

- **주간 창은 달력 주가 아니다.** `seven_day.resets_at` 에서 7일 뺀 순간이 창의 시작이고,
  **창이 흐른 비율이 곧 알맞은 사용률(`due`)** 이다 — 2/7 흘렀으면 28% 가 제 속도.
  그래서 요일·시각을 어림잡을 필요가 없다(사람이 "토요일 11시면 2/7" 하고 세던 걸 정확히 계산).
- **5시간 한도의 판정(여유/빠름)은 안 낸다.** 그 창은 첫 메시지에 열려 앞쪽에 몰아 쓰는 게
  정상이라 '고르게 쓴다'를 전제로 한 판정이 뜻을 못 가진다. **다만 눈금(적정선)은 5시간에도
  긋는다** — `cooldown_core.five_due` 가 창이 흐른 비율(0~100)만 돌려주고(판정 없음), 채운 양과
  견주면 '이대로면 초기화 전에 바닥나겠다'를 한눈에 보게 해 준다. 창이 없거나 이미 풀렸으면 None(숨김).
- 판정은 **'며칠 앞섰나'** 로 잡는다(`DAY_PP` = 하루치 14.3%p):
  하루치 이상 덜 씀 `여유` / 반나절 앞까지 `알맞음` / 하루반까지 `조금 빠름` / 그 위 `많이 빠름`
  / 99.5% 이상 `다 씀`. 색은 `level`(0·1·2)로 넘기고 고르는 건 화면 쪽(`skins.base.pace_color`).
- **`projected`·`runout` 은 창이 15%(약 하루) 흐른 뒤부터만 준다.** 창이 막 열렸을 땐
  몇 분치 속도가 그대로 늘어나 '이 속도면 2000%' · '4분 뒤 소진' 같은 헛말이 나온다.
- 화면:
  - **모든 스킨의 5시간·주간 게이지에 '지금쯤' 눈금**(`skins.base.mark_x`/`MARK_W`, 색은 `P.title`
    고정). 주간은 `pace().due`, 5시간은 `five_due()` 로 자리를 잡는다(둘 다 창이 흐른 비율).
    게이지 색(초록/노랑/빨강)을 쓰면 값처럼 보여 두 개를 헷갈린다. **눈금은 게이지 위아래로 조금
    삐져나오게** 긋는다 — 칸 나뉜 게이지(슬림·표)에서 틈에 떨어지면 안 보이기 때문.
    (스킨의 게이지 그리는 쪽은 `set(pct, left, due)` 로 눈금 자리를 받는다. 카드형만 주간이
     `Pace` 객체를 받아 판정 글자까지 그리므로 `set(..., p=..., due=...)` 로 나뉜다.)
  - 숫자·판정은 **카드형**(주간 줄 오른쪽 `적정선 67% · 알맞음`) · **트레이 툴팁** ·
    **우클릭 > `이번 주 사용 속도…`** 팝업. 슬림 바는 자리가 없어 눈금만 선다.
  - **용어는 '적정선'으로 통일**(옛 '지금쯤'). due = 창이 흐른 만큼 = 고르게 썼다면 지금 여기.
    카드·트레이는 짧게 `적정선`, 팝업은 자리가 있어 `적정선 (고르게 쓸 때)` 로 스스로를 설명한다.
- **사용 속도 팝업(`open_pace`)은 말로 풀어 준다**(처음 보는 사람이 바로 알게). 판정 밑에
  `고르게 쓸 때보다 N%p 덜/더 썼어요`(`_gap_sentence`) 한 줄, `지금까지 사용` ↔ `적정선` 두 값,
  그 아래 `이대로 계속 쓰면 → 초기화 때 X%`(projected) · `하루 이만큼까지 OK X%`(per_day) ·
  `이대로면 다 쓰는 때`(runout) · `초기화`. **'지금쯤/지금' 처럼 겹쳐 헷갈리는 이름, '주 끝' 같은
  모호한 말, 두 값을 한 칸에 몰아넣기(옛 `남은 3일 · 하루`)를 되살리지 말 것.**
- 확인: `python pc/cooldown_core.py` 끝줄 (`주간 15% · 적정선 9% · 알맞음`).

## 사용량 통계 (`cooldown_stats`)

우클릭 > **`사용량 통계…`**(트레이에도 있음). 지나간 기록으로 '언제 얼마나 썼나' 를 본다 —
`이번 주 사용 속도` 가 **지금 이 창**을 보는 것이라면, 이쪽은 **쌓인 날들**을 본다.

- **사용량 = 주간 퍼센트가 오른 만큼(%p).** 5시간 퍼센트는 5시간마다 0으로 돌아가 더할 수가
  없지만, 주간 퍼센트는 7일 창 안에서 늘기만 하므로 그 증가분이 곧 그 사이에 쓴 양이다.
  창이 바뀌는 순간(초기화)만 따로 다룬다 — 그때는 퍼센트가 떨어지므로 뺄셈 대신
  **새 창에서 지금까지 쓴 양**을 증가분으로 본다(`deltas`).
- 기록은 `~/.claude_cooldown_history.jsonl` — 한 줄에 한 표본
  `[찍은때, 5시간%, 주간%, 5시간초기화, 주간초기화]`(전부 epoch 초). 조회에 성공할 때마다
  `record()` 가 쓰는데, **값이 그대로면 안 쓴다**(클로드를 안 쓰면 퍼센트가 안 변한다).
  다만 `HEARTBEAT`(30분)마다 한 줄은 남겨 **기록이 끊긴 구간(컴퓨터 꺼짐)과 '안 쓴 구간'을
  가른다.** 켤 때 `trim()` 이 `KEEP_DAYS`(120일)보다 오래된 줄을 덜어낸다.
- **위젯이 떠 있는 동안에만 쌓인다** — 소급 계산할 원본이 없으므로 처음 켠 날부터 시작한다.
  표본이 2개 미만이면 팝업은 `쌓인 기록 없음` 한 줄만 보여 준다.
- 화면(`App.open_stats`)은 셈을 하지 않는다. `analyze()` 가 돌려주는 `Report` 를 그리기만:
  · **일별 사용량** 막대 14일 + **하루 적정선**(`DAY_PP` = 100/7 = 14.3%p) 점선.
    막대 색은 적정선과 견준 등급(`App._day_level` → `pace_color`)이라 주간 판정과 잣대가 같다.
  · **시간대** 0~23시 막대(가장 많은 때만 `P.title` 로 밝게) · **5시간 창 최고** 최근 12개
    (0~100% 고정 눈금 + `P.track` 뒷판이라 창끼리 바로 견준다).
- 확인: `python pc/cooldown_stats.py` (콘솔 요약 + 일별 막대).

## 자동 핑 (모닝 스타터)

> **용어 주의:** 코드·이 문서에서는 '핑/앵커' 를 쓰지만, **사용자 화면(메뉴·팝업·트레이·알림)에는
> '핑' 같은 개발자 용어를 노출하지 않는다.** UI 어휘는 '자동 시작 / 5시간 자동 시작 / 실행'.
> ★ **딱 한 군데 예외: 위젯의 번갈아 칸**(`App._notice_text`) — 60px 도 안 되는 자리라
> `20:03 자동 시작 실패` 가 안 들어간다. 거기서만 `20:03 핑 실패` · `20:03 핑 놓침` 으로 줄여 쓴다
> (2026-08-03, 쓰는 사람이 그렇게 해 달라고 정한 것). 메뉴·팝업·트레이는 그대로 '자동 시작'.

5시간 창은 '첫 메시지 순간'부터 5시간이다. 정한 앵커 시각(기본 **05:00·10:01·15:02·20:03**)에
토큰을 거의 안 쓰는 핑을 자동으로 보내 창의 경계를 그 시각들에 맞춰 둔다 —
언제 초기화되는지 예측이 된다. 위젯 안에서 돈다(예약작업 schtasks 불필요).

- **간격 규칙**: 두 시각은 **5시간 1분(`MIN_GAP_MIN=301`) 이상** 벌어져야 한다(자정 넘김 원형 검사).
  5시간 창은 정확히 5시간이라, 이전 창이 끝난 '직후'에 열리게 1분씩 벌려 둔 게 기본값의 :01·:02·:03.
  하루 최대 `MAX_TIMES=4`(5×301>1440). 검증은 `cooldown_ping.gap_error()`, 어기면 저장 거부·안내.
  옛 05·10·15·20(정확히 5시간)은 `load_cfg` 가 기본값으로 자동 이전한다.
  (`MIN_GAP_MIN=301` 은 `GRACE_MIN=5` 보다 작지만, 늦게 쏜 핑도 `delta` 가 앵커마다 1분씩 줄어
  다음 앵커 여유 안에 들어와 자기보정되므로 건너뜀이 안 난다 — 리뷰로 확인.)

- **전송은 `claude -p` 헤드리스**로 한다(브라우저 자동화 아님 — 옛 실패작 폐기).
  이미 로그인돼 있고 구독 한도를 공유하므로 5시간 창이 정확히 열린다.
  CLI 는 OAuth 토큰을 **스스로 갱신**하므로 위젯 직접 조회가 401 이어도 핑은 나간다.
  기본 모델은 `claude-haiku-4-5-*`(주간 한도를 덜 깎음), 프롬프트는 한 단어만 답하게.
- **하이브리드 규칙** — 핑은 **앵커 시각에만** 쏜다. 그 순간 5시간 창이 활성이면
  (`five.resets_at` 이 미래) **건너뛴다.** 활성 창에 메시지를 더 보내도 경계가
  밀리지 않고 토큰만 쓰기 때문. 창이 풀린 뒤 다음 앵커에 다시 정렬된다(오프그리드 핑 0).
  단, 사용자가 앵커가 아닌 시각에 직접 클로드를 쓰면 그 창만큼 경계가 밀린다 — 구조적 한계.
- 판단은 `pc/windows/cooldown_app.py` 의 `_ping_tick`(20초마다) → `cooldown_ping.should_ping_now`.
  전송은 별도 스레드(`_start_ping`), 결과는 `ping_out` 큐로 받아 `_on_ping_result`.
  **같은 앵커 재전송 방지**: 자동 핑은 쏘기 전에 낙관적으로 `last_ping` 을 지금으로 세운다.
  실패하면 그 앵커는 건너뛴다(다음 앵커가 다시 정렬).
- **창 상태를 모르면(첫 조회 전 `last_usage is None`) 자동으로 쏘지 않는다** —
  활성 창인데 모르고 쏘면 토큰만 버린다. 수동 '지금 핑 보내기' 는 상태와 무관하게 쏜다.
- **실패·놓친 앵커 표시** — 컴퓨터가 꺼져 있던 등으로 앱이 안 떠 있는 동안 앵커의 여유(grace)
  구간이 다 지나가면 그 앵커는 핑도 못 쏘고 처리 기록도 못 남긴다 = **놓침**. 쏘긴 쐈는데 CLI 가
  실패한 것 = **실패**. 둘 다 위젯 게이지 근처(스킨별 꼬리말·제목·번갈아 칸)에 호박색으로
  `HH:MM 핑 실패` / `HH:MM 핑 놓침`(위 용어 예외), 트레이 알림·툴팁에는 '자동 시작' 으로 남긴다.
  - **실패가 놓침보다 앞선다**(`_notice_text`) — 지금 창이 안 열린 게 더 급하다. 위젯 문구에는
    **까닭을 붙이지 않는다**(자리가 없다. 까닭은 트레이 알림과 `실행 기록…` 에 있다).
    실패 표시는 `self._ping_fail`(메모리에만) 이고 **다음 성공 · 기능 끄기 · `실행 기록…` 열기**
    로 지운다. 파일에 저장하지 않는다 — 다시 켜서까지 옛 실패를 띄울 일이 아니다.
  - 판정은 `cooldown_ping.missed_since`(순수 함수). 앱이 떠 있던 앵커는 놓침이 아니다 —
    `_ping_tick` 이 여유 구간에 있으면(`anchor_in_grace`) 핑을 쏘든 창이 활성이라 건너뛰든
    **`last_anchor`(처리한 앵커)** 에 기록해 둔다. 놓침은 `last_anchor`·`last_ping` **뒤에** 있고,
    지금 활성 창이 그 앵커를 덮지 않은 것만 잡힌다(창이 덮으면 의도적 건너뜀이라 놓침 아님).
  - **놓침은 다음 자동 시작이 성공하면 지운다**(창이 다시 정렬됨). 자동 핑을 켠 순간엔
    직전 앵커를 기준점(`last_anchor`)으로 잡아 **이전 것을 놓친 걸로 오인하지 않는다**.
    이 기능 켜고 아무 기록도 없는 첫 순간도 기준점만 잡고 넘어간다(오인 방지).
  - 스킨은 `Skin.notice(text)` 로 표시한다(기본 no-op). **오류보다 낮은 우선순위** —
    앱은 오류가 없을 때만, `show()` 직후에 부른다. `text` 가 비면 스킨은 아무것도 안 한다
    (직전 `show()` 가 그 자리를 정상값으로 이미 되돌렸으므로). 자리를 새로 만들지 말 것(높이 고정).
  - 설정: `~/.claude_cooldown_ping.json` 의 `last_anchor`·`last_missed`(둘 다 ISO 로컬).
- 설정 파일 `~/.claude_cooldown_ping.json`, 로그 `~/.claude_cooldown_ping.log`,
  핑 실행 폴더 `~/.claude_cooldown_ping_cwd`(빈 폴더 — 큰 CLAUDE.md 를 딸려 읽어
  입력 토큰이 늘지 않게 일부러 여기서 실행).
- 우클릭 메뉴는 **두 진입점**으로 나뉜다(한 앱, 기능은 분리):
  **쿨다운 (사용량 표시)** = 위젯 모양·동작(새로고침·디자인·밝기·항상위, `menu_cool`).
  **'작업표시줄에 붙이기' 토글은 없다** — 디자인에서 슬림 바를 고르면 자동으로 붙는다(아래 참고).
  **모닝 스타터 (5시간 자동 시작)** = 체크박스 `정한 시각마다 5시간 자동 시작`(`toggle_ping`) ·
  `지금 한 번 실행`(`send_ping_now`) · `시각 설정…`(`open_ping_times`) · `실행 기록…`(`open_ping_log`).
  자동 실행·종료만 메인에. **메뉴엔 동적 값(다음 시각·마지막 결과)을 나열하지 않는다** — 설정·기록 창에서 본다.
  (그래서 index 기반 갱신 `_sync_ping_menu`·`menu_ping` 은 없음. 되살리지 말 것.)
- 실패 원인은 `cooldown_ping.friendly_error()` 로 **한국어 명사형**으로 바꿔 보여 준다
  (영문 stderr·'오류 코드 N' 원문은 로그 파일에만). 트레이 알림·실행 기록 둘 다 이걸 쓴다.
- **팝업은 고정 크기라 내용이 늘면 잘린다** — 행 추가/삭제·경고문 표시 뒤 `_refit_panel(top, W)` 로
  높이를 다시 잰다(안 하면 저장 버튼·경고문이 창 밖으로 나가 안 보임). 시각 편집은 엔터로도 저장.
  시각 입력은 **`tk.Spinbox`**(시 0~23·분 0~59, `wrap`) — 직접 타이핑·화살표·길게눌러 반복
  (`repeatdelay`/`repeatinterval`)·스크롤 다 됨. `textvariable` trace 로 data 에 반영,
  포커스 아웃 때 두 자리로 정돈. 분은 1분 단위(간격 규칙 5시간 1분을 맞추려면 분 단위 조절 필요).
- 단독 확인: `python pc/cooldown_ping.py`(판단만) / `python pc/cooldown_ping.py --send`(실제 1회 전송).

## 색 (밝게 / 어둡게)

- 색은 `skins/base.py` 에 **두 벌**(`DARK`, `LIGHT`) 있고, 지금 쓰는 한 벌이 `P` 다.
- 스킨은 **반드시 `from .base import P` 로 객체째 받아 `P.bg` 처럼 쓴다.**
  `from .base import BG` 처럼 값을 직접 가져오면 그 시점 색이 굳어 테마가 안 바뀐다.
  테마 전환은 `P` 의 값만 덮어쓰므로, 객체로 받아 둔 쪽은 자동으로 따라온다.
- 색은 만들 때 위젯에 박히므로, 전환하려면 **다시 그려야 한다**
  (`App._apply_theme` 가 `_build_body` → `_replay` 를 부른다).
- `theme` 설정은 `auto` / `light` / `dark`. `auto` 면 윈도우의 `AppsUseLightTheme`
  를 4초마다 확인해 따라간다.
- 새 색을 넣을 때는 **두 벌 다** 채우고 배경 대비 4.5:1 을 지킬 것.

## 화면 규칙 (스킨을 만들거나 고칠 때)

- 새 스킨은 `base.Skin` 상속 + `skins/__init__.py` 의 `_MODULES` 에 한 줄 추가.
- **네 상태(정상/연결실패/재로그인/불러오는중)의 창 높이가 같아야 한다.**
  앱이 처음 잰 높이로 창을 고정하므로 늘어나면 잘린다. 오류는 자리를 새로
  만들지 말고 기존 자리의 색·글자를 바꿔서 표현한다.
- `show_error(text, keep_values, stamp)` 의 `keep_values` 가 참이면
  숫자·게이지를 건드리지 않는다 (일시적 연결 실패 — 마지막 값을 남긴다).
- 흐린 글자도 배경 대비 **4.5:1 이상**. 위계는 밝기가 아니라 글자 크기로 준다.
- 게이지 칸 계산은 `int(pct * n / 100)` (내림). `round` 를 쓰면 99% 와 100% 가
  구별되지 않는다.
- 확인: `python -u pc/windows/_shot_skin.py <키> {ok|net|err|max|note} out.png`
  (`max` 는 100% · 가장 긴 문자열 · 긴 모델명인 최악 케이스, `note` 는 값은 멀쩡한데
   자동 시작 알림이 떠 있는 상태)

## 앱이 꺼졌을 때 (블랙박스)

`pythonw`·exe(`--windowed`)는 콘솔이 없어 **오류가 어디에도 안 남는다** — 앱이 소리 없이
사라져도 원인을 알 길이 없었다. 그래서 `~/.claude_cooldown_app.log` 에 켜고·끄고·죽는
순간을 남긴다. **꺼졌다는 얘기가 나오면 여기부터 본다.**

- `시작` / `종료 — 메뉴`(사용자가 끔) / `종료 — 창이 닫힘` / `종료 — 인터프리터 정리`.
  **`종료 —` 줄 없이 다음 `시작` 이 나오면 밖에서 죽은 것**(강제 종료·보안 솔루션 등).
- `치명적 오류`(메인) · `스레드 오류`(폴러·핑) · `화면 오류`(Tk 콜백) 는 traceback 째로 남는다.
  특히 **Tk 콜백 오류는 원래 stderr 로 새어 나가 증발**했다 — `report_callback_exception` 을 갈아 끼웠다.
- `faulthandler` 를 같은 파일에 걸어 둬서, 파이썬이 통째로 죽으면(`Fatal Python error`)
  마지막 스택까지 남는다. 로그 파일 핸들은 열어 둔 채로 두고 **줄이기는 시작할 때 한 번만** 한다.
- **`quit()` 은 `os._exit(0)` 로 끝낸다.** 데몬 스레드(트레이·폴러·핑)가 살아 있는 채로
  인터프리터를 정리하면 파이썬이 `abort()` 로 죽는다 — 실제로 2026-07-29 00:04·00:05 에
  WER 에 `c0000409` / `FATAL_APP_EXIT` 로 두 번 찍혔다. 저장은 `os._exit` 앞에서 이미 끝낸다.
- 윈도우 쪽 증거를 볼 곳: 이벤트 뷰어 `Application Error`, 덤프 `%LOCALAPPDATA%\CrashDumps`,
  보고서 `%PROGRAMDATA%\Microsoft\Windows\WER\ReportArchive`. **여기에도 없고 로그에도
  `종료 —` 가 없으면** 네이티브 크래시가 아니라 밖에서 죽인 것이다.

## 동작 규칙 (건드릴 때 주의)

- **새로고침 표시는 스피너** — 위젯을 클릭하면 새로고침되고, 오른쪽 위에 작은 캔버스
  스피너(`_spin_once`, 배경 링+도는 조각)가 한 바퀴 돈다. 예전 '창 흐리기(BUSY_ALPHA)'
  깜빡임은 뺐다. 우클릭 메뉴의 '지금 새로고침' 항목도 뺐다(클릭이 곧 새로고침). 트레이엔 남겨 둠.
- **중복 실행 금지** — 이름 있는 뮤텍스로 판정. 두 번째 프로세스는 창을 띄우지 않고
  `~/.claude_cooldown.summon` 파일만 남기고 끝난다. 떠 있는 쪽이 `_pump` 에서
  그 파일을 보고 앞으로 나온다. (시작 프로그램·바로가기·.bat 이 겹쳐 눌리기 쉽다)
- **재시도 간격** — 폴링은 300초 고정이지만, `ConnectionFailed`(서버에 닿지도
  못함)일 때만 20 → 40 → 80 → 160 → 300 초로 빨리 되묻는다. 요청이 서버에
  도달하지 않았으므로 rate limit 과 무관하다. **429·로그인 만료·정상은 300초를 지킨다.**
- 한도 알림은 5시간·주간을 따로 센다(`self.warned` 딕셔너리). 주간이 차면 며칠을
  묶이므로 이쪽이 더 아프다.
- 창 위치는 저장·복원할 때 `clamp_to_screen()` 을 통과시킨다. 제목표시줄이 없어
  화면 밖으로 나가면 끌어올 수가 없다. 기준은 화면 전체가 아니라 **작업 영역**이다
  (전체 기준이면 작업표시줄에 덮여 12px 만 남는다).
- **`_release` 는 실제로 움직였을 때만 위치를 저장하고 붙이기를 푼다**(`DRAG_SLOP`).
  안 그러면 붙여 둔 위젯을 한 번 클릭했을 뿐인데 작업표시줄 뒤로 내려간다.
  **붙여 둔 동안에는 임계값이 `UNDOCK_SLOP`(120px)** — 클릭하다 몇 px 밀린 정도는 클릭으로 보고
  `_reassert_dock` 으로 제자리에 다시 붙인다. 그래야 새로고침하려고 누를 때마다 작업표시줄에서
  떨어져 위로 뜨지 않는다(떼려면 크게 끌면 된다. **다시 붙이려면 디자인에서 슬림 바를 다시 고른다**
  — 붙이기 토글을 없앴으므로 `switch_skin` 이 같은 키 재선택 시 되붙인다). `DRAG_SLOP` 4px 만으론 부족했다.
- **붙어 있는 동안에는 x/y 를 저장하지 않는다**(`_remember_spot`). 작업표시줄
  좌표가 자유 위치를 덮으면 나중에 풀었을 때 화면 끝에 걸린다.
- **`_pump` 은 try/finally 로 감싸 무슨 일이 있어도 다시 예약한다.** 여기서
  예외가 새면 갱신도 멈추고 트레이의 '종료' 마저 안 먹는다(같은 큐를 쓴다).
- 오류 문구는 `cooldown_core` 가 **짧은 명사형**으로 던진다. 슬림 바의 오류 자리가
  91px 뿐이라 길면 잘린다. 새 문구를 만들 때 폭을 재 볼 것.
- 응답 필드 이름만 바뀌어도 예외가 안 나고 값만 전부 None 이 된다. `parse` 가
  이걸 잡아 '형식 변경' 으로 올린다 — 안 잡으면 화면에 '--' 만 뜨고 고장난 줄 모른다.
- 오류가 나도 **마지막 값은 남긴다**(`keep_values`). 토큰이 8시간마다 만료돼
  자고 일어나면 매번 걸리는데, 그때마다 지우면 정작 궁금한 걸 못 본다.
  값이 언제 것인지는 기준 시각이 보여 준다 — 스킨은 오류 상태에서
  **실패한 시각이 아니라 마지막 성공 시각**을 써야 한다.
  (**슬림 바만 예외** — 기준 시각 대신 초록/빨강 상태 점을 쓴다. 위 '작업표시줄에 붙이기' 참고)

## 작업표시줄에 붙이기

- **기본값이 '슬림 바 + 붙임'이다** (`load_state` 의 `skin="slim"`·`dock=True`, `skins.DEFAULT="slim"`).
  처음 켜면 바로 작업표시줄에 붙는다. 별도 '붙이기' 메뉴 토글은 없앴다.
- 윈도우 11 은 작업표시줄 **안에** 넣는 길(데스크밴드)을 없앴다. 위에 겹쳐 놓는다.
- `dockable = True` 인 스킨에서만 쓸 수 있다 (지금은 `slim` 뿐).
  작업표시줄 높이에 맞춰 그리지 않는 스킨이 붙으면 삐져나오기 때문.
- **붙임은 '디자인 = 슬림 바'와 한 몸이다.** `switch_skin` 이 `dock = 새 스킨.dockable` 로 정한다:
  슬림 바를 고르면 붙고, 다른 디자인을 고르면 풀린다. 끌어서 떼어 내면 `dock=False`(그 자리 유지),
  다시 붙이려면 **슬림 바를 다시 선택**한다(같은 키 재선택 시 `switch_skin` 이 되붙인다).
- 슬림 바: 왼쪽 끝 **상태 띠(`accent`, 한도 색 초록/노랑/빨강)** + **통통 튀는 도트 마스코트 '클로디'**
  (Claudi — 슬림 스킨에 사는 코랄색 캐릭터).
  - **오른쪽은 '번갈아 칸' + '상태 점' 두 가지다**(`SLOT_*` 상수, `_slot_*` 메서드).
    **옛 '03:07 기준' 줄은 없앴다** — 시각은 작업표시줄 시계에 이미 있고, 정작 알고 싶은
    '이 숫자가 지금 것인가'는 점 하나로 충분하다(조회되면 초록, 못 받으면 빨강).
  - **상태 점은 오른쪽 위 구석**(`DOT_INSET`)이고 **그리는 건 앱이 한다** — 스킨은
    `status_spot() → (x, y, 바탕색)` 으로 자리만 알려 준다. 까닭: **새로고침 스피너가 그 점
    둘레를 도는 링**이라 둘을 한 캔버스(`App._status`, `STATUS_BOX`)에 둬야 겹쳐도 조화롭다
    (tk 위젯은 투명이 안 돼 포개면 아래가 가려진다). `status_spot()` 이 `None` 인 스킨
    (카드·아크·표 — 기준 시각을 글자로 보여 준다)은 옛날처럼 구석에 링만 잠깐 뜬다.
    자리·색은 `App._sync_status()` 가 `_build_body`·`_show`·`_show_error` 에서 다시 잡는다.
  - 번갈아 칸은 한 줄뿐인데 보여 줄 것이 여럿이라 `SLOT_HOLD`(4.2초)마다 하나씩 바뀐다 —
    보여 주던 것이 위로 밀리며 바탕색에 스미고(`_mix`. tk 캔버스 글자엔 투명도가 없어 색을
    바탕 쪽으로 당겨 흉내 낸다) 다음 것이 아래에서 떠오른다.
    도는 것: **모델별 한도(`Fable 7%`) → 알림**, 오류일 땐 오류 하나.
    ★ **적정선(`적정선 62%`)은 넣지 않는다** — 게이지에 ┃ 눈금으로 이미 서 있어서 숫자를
      또 적을 까닭이 없다(넣었다가 뺐다). 숫자로 보고 싶으면 우클릭 > 이번 주 사용 속도.
  - **글자는 아래 정렬**이다 — 밑선을 게이지 아래끝(`div_bot`)에 맞춘다. 위아래 가운데에
    띄우면 옆 두 칸의 두 줄 사이에 붕 떠 보인다(그렇게 했다가 고쳤다).
  - ★★ **칸보다 긴 글자는 자르지 않고 좌우로 훑는다**(`_slot_pan_step`) — 머리를 보여 주고
    잠깐(`PAN_WAIT`) 멈췄다가 `PAN_SPEED` 로 밀어 꼬리까지 보여 준 뒤 또 멈춘다. 덕분에
    `Claude Opus 99%`·`재로그인 필요` 도 다 읽히고, **칸 폭을 최악 문구에 맞춰 넓힐 까닭이
    없어졌다**(그래서 창 폭을 520 → 480 → **460**으로 되돌렸다. 더 줄이면 두 한도 칸의
    `23시간 59분 후` 부터 잘린다 — `need_cell` 이 162px×2 로 폭의 대부분을 먹는다).
    - ★ 훑는 글자가 마스코트·게이지 위로 흘러넘치지 않게 **이 칸만 따로 캔버스**(`self.slot`)로
      만들어 `create_window` 로 얹는다. tk 캔버스는 **도형 단위 클리핑이 없고** 제 테두리에서만
      자르므로, 한 캔버스에 그리면 막을 길이 없다. 테마를 바꿀 때 `_paint` 가 **이 캔버스 바탕색도
      같이** 갈아야 한다(안 그러면 네모난 자국이 남는다).
    - ★ 몇 px 만 넘칠 때도 `PAN_MIN_FRAMES`(약 0.5초)를 들여 민다 — 속도대로 하면 4px 를
      0.1초에 밀어 글자가 흠칫 떠는 것처럼 보인다.
    - ★ `PAN_MAX`(칸 폭의 2.8배)를 넘는 글자는 `_fit` 으로 … 줄여서 들인다. 안 그러면
      하염없이 흐른다.
  - `show()` 가 늘 있는 것들을, `notice()` 가 알림을 세운다. 둘은 잇달아 불리므로
    **바로 반영하지 않고 `after_idle` 로 한 번만 반영한다**(`_slot_queue`/`_slot_commit`) —
    안 그러면 갱신(1분마다)마다 알림이 사라졌다 다시 붙어 순서가 처음으로 되감긴다.
    **내용이 그대로면 돌던 자리를 지킨다.** 알림이 새로 뜨면 순서를 기다리지 않고 먼저 보여 준다.
    ★ `show_error` 는 예약된 반영을 **`_slot_cancel()` 로 물려야 한다** — `None` 으로 두기만
    하면 예약이 살아 있다가 오류 문구를 옛 목록으로 덮어쓴다.
  - 애니메이션은 마스코트처럼 **세대 토큰(`_slot_gen`)** 으로 옛 루프를 은퇴시킨다.
    build 가 다시 불려도(테마 전환) 세대는 **올리기만** 한다 — 0 으로 되돌리면 옛 캔버스에
    걸려 있던 콜백과 번호가 겹쳐 두 루프가 같이 돈다.
  - ★ `MGAP`(주간 칸 ↔ 번갈아 칸 틈, 42)을 **더 줄이지 말 것** — 마스코트는 팔까지 11칸(22px)
    이지만 숨쉬기·기지개로 부풀고 계단식 기울임으로 윗줄이 옆으로 밀려 실제로는 30px 남짓 쓴다.
  - **마스코트 위치는 주간 칸과 오른쪽(모델/기준) 사이 빈 틈**이다. 가로 배치는
    `[띠] 5시간 |GAP| 주간 |MGAP(마스코트)| 오른쪽칸`. **오른쪽 칸 폭(`right_w`)을 먼저 확보**하므로
    `Claude Opus 99%` 처럼 긴 모델명이 와도(→`_fit` 이 `…` 로 줄임) 마스코트와 안 겹친다.
    `mascot_cx` 는 build 에서 그 틈 가운데로 계산한다(고정 상수 아님).
  - 띠가 상태를 맡으므로 **마스코트는 상태와 무관하게 클로드 코랄색(`MASCOT_COLOR`)으로 고정**한다.
  - 애니메이션 루프는 `_start_mascot`→`_animate`(`FRAME_MS`=45ms)→
    `_step_physics`→`_idle_step`→`_draw_mascot`(매 프레임 `delete("mascot")` 후 다시 그림). 색은 안 바꾼다.
  - **생김새는 도트(픽셀) 친구다** (2026-08-02. 옛 '별빛'은 이 크기에서 얼굴이 안 살았다) —
    **몸통 없이** 큰 머리에 **팔 둘·다리 둘**이 바로 붙었고(Clawd 결), 얼굴은 네모 눈 둘과 입.
    그림은 `HEAD`/`LEGS`/`ARM`/`EYES`/`MOUTH` **표에 `#` 로 찍어 두고**, 픽셀 변환은
    `_sprite` 한 곳에서만 한다 (칸 = `MASCOT_U`=2px, 팔까지 13칸 × 9줄 → 26×18px).
  - **팔은 왼팔만 적고 오른팔은 `8 - col` 로 뒤집어 쓴다** — 그래서 `arms=(왼, 오른)` 처럼
    한쪽만 들 수 있고, 그걸로 **손 흔들기**(`_wave`) 잔동작이 나온다.
  - ★ **머리는 9칸이라야 한다.** 7칸으로 줄이면 눈 구멍 둘이 살을 다 먹어 머리가 가로로
    갈라져 보인다(게 집게처럼). ★ **눈줄과 입줄 사이에 살이 한 줄** 있어야 한다 — 붙이면
    두 구멍이 이어져 얼굴이 아니라 커다란 아가리 하나로 읽힌다. ★ **팔은 눈 아래에서**
    나간다. 눈 높이에 걸치면 수염처럼 보인다. 셋 다 실제로 겪고 고친 것이다.
  - ★ **눈웃음은 ∧∧ 가 아니라 넓고 낮은 띠**(가늘게 뜬 눈)다. ∧ 를 칸으로 찍으면 떨어진
    점 셋으로 보여 얼굴이 아니라 얼룩이 된다. 같은 이유로 놀란 눈은 **3칸으로 넓힌 네모**다.
  - **기울임은 회전이 아니라 계단식(`lean`)** — 윗줄일수록 옆으로 더 민다. 도형을 돌리면
    이 크기에서 도트 결이 뭉개진다. 눈 굴리기(`_look`)도 **칸 단위**로 옮긴다.
  - 몸통·다리는 **줄마다 이어진 칸을 한 덩이로**(`_sprite` 의 `paint`) 그린다 — 도형 수가
    프레임당 84 → 42 로 준다. 경계 식이 낱칸과 같아 그림은 한 픽셀도 안 달라진다.
  - **심심할 때 잔동작(`_idle_step`/`_begin_gesture`)**: 쉬는 동안 이따금 하나씩 —
    깜빡(`_blink`)·**손 흔들기(`_wave`, 한 팔만 네 프레임마다 오르내림)**·눈 굴리기(`_look`,
    눈만 옆으로)·고개 갸웃(`_tilt`)·기지개(`_stretch`, 세로로 쭉 늘리며 **팔도 번쩍**)·
    부르르(`_wiggle`)·반짝이 뿜기(`_sparks`, 도트 십자가 떠오르며 사그라듦)·가끔 폴짝(`hop`). `숨쉬기(breathe)`+잔잔한 통통은 항상. 반응으로 출렁이거나(`moving`) 다른 잔동작
    중이면 새 잔동작은 안 낸다.
  - **뛰는 높이는 창 높이에서 계산한다**(`_step_physics` 의 `lift`) — 도트 그림 높이(`SPRITE_H`)를
    빼고 남는 만큼만 올라가므로 작업표시줄이 40px 든 72px 든 창 밖으로 안 나간다.
  - ★★ **드래그·우클릭 바인딩은 창(toplevel)에만 건다**(`_build_body` → `_bind_drag(self.root)`).
    tk 이벤트는 자식에서 창으로 올라오므로 그것만으로 다 잡힌다. 자식마다 또 걸면
    **한 번 누른 게 두 번 처리된다** — 2026-08-03 까지 실제로 그러고 있었고, 그래서
    마스코트가 두 배로 튀고(콕 한 번에 임펄스 2회) 기절 누적도 두 배로 쌓였으며
    **클릭 한 번에 반짝이가 터졌다**(타이밍 조건이 늘 만족돼서). 되살리지 말 것.
  - ★★ **몸을 좌우로 흔들지 않는다.** 늘 살랑거리는 기울임(옛 `sin(t*0.05)`)·누를 때 도는
    힘(`SPIN_IMPULSE`=0)·부르르(`wiggle`)를 **전부 뺐다** — 산만하고 안 예뻤다. 남은 좌우
    움직임은 **가끔 하는 고개 갸웃(`_tilt`)** 과 노트북 볼 때 기울이는 것(`TYPE_LEAN`) 뿐이다.
  - **누르면 반응한다 — 용수철 모델**: `App._press` 가 창 기준 좌표로 `self.skin.react(x, y)` 를 부르면
    위로 튀는 **속도**(`_vy`)를 더한다. `_step_physics` 가 매 프레임 제자리로 당겨(`SPRING_K`/`SPRING_DAMP`)
    **연타하면 힘이 쌓여 부드럽게 출렁**인다(뚝 끊기던 옛 카운트다운 방식을 버림).
  - **직접 콕 vs 아무 데 구분**: `react` 가 `_hit(x,y)`(마스코트 반경 `HIT_R`)로 판단 —
    **직접 찌르면** 크게 펄쩍(`POKE_MULT`) + `_surprise` 동안 눈이 O O(놀람) + `_clicks` 누적,
    **딴 데면** 잔잔히 통통만 하고 **기절에는 안 쌓인다**(`_clicks` 미변경) + 움직이는 동안 ∧∧ 눈웃음.
  - **과부하 기절 — 직접 콕만**: 직접 콕 누적(`_clicks`, 매 프레임 `CLICK_DECAY` 로 식음)이
    `FAINT_AT`(**90 — 작정하고 12초쯤 두드려야 한다.** 어쩌다 뻗으면 놀란다)를 넘으면 `_faint` 상태로 `FAINT_FRAMES`(~2.7초) 동안 `_draw_faint` — **`FAINT_SCALE` 로 칸을
    키운 X_X 눈** + 팔은 축 늘어지고 다리가 벌어지며(`LEGS_WIDE`) 노란 별이 머리 위를 돈다.
    그동안 `react()` 무시, 깨어나면 리셋하고 반짝이를 펑 뿜는다.
    ★ **`FAINT_SCALE` 은 1.15 다** — 1.35 로 키웠더니 딴 친구가 나타난 것처럼 커 보였다.
      눈이 보일 만큼만 키운다(폰 `Mascot.kt` 도 같은 값).
  - **꾹 누르면 눌린다**(`hold`/`let_go`) — 앱이 `HOLD_MS`(0.34초) 뒤에 `skin.hold(x, y)` 를
    부르고, 떼거나 끌기로 바뀌면 `skin.let_go()` 를 부른다. 누르고 있는 동안 눈을 질끈 감고
    **점점 납작해지다가**(`PRESS_FLAT`), 떼면 눌린 만큼 **용수철처럼 튕겨 오른다**(`PRESS_POP`).
    ★★ **여기에 '기 모아 뛰기' · '기 모아 쏘기' · '쓰다듬기(하트)' 를 차례로 붙여 봤다가
      전부 뺐다.** 소품이나 기호를 얹으면 게임 같거나 유치했다 — **누르니까 눌린다**,
      그 이상 필요 없다. 새 아이디어를 넣기 전에 이 셋을 이미 버렸다는 걸 기억할 것.
  - ★★ **튀는 힘에는 상한(`MAX_VY`)이 있고, 끝에 닿으면 속도까지 죽인다.** 위치만 가두면
    연타할 때 속도가 계속 쌓여 **천장에 붙은 채 한참 못 내려온다** — 그게 '연타하면 마비된
    것 같다' 의 정체였다(2026-08-03). 되살리지 말 것.
  - **점프의 손맛은 스쿼시·스트레치다**(`SQUASH*`) — 튀어오를 때 세로로 늘고 내려앉을 때
    눌린다. 이게 없으면 그냥 위아래로 미끄러질 뿐이라 뛰는 맛이 없다(빼 봤다가 되살렸다).
    **세로로만** 한다 — 좌우로 흔드는 건 안 쓴다.
  - ★★ **박자 맞히기(콤보) — 5단.** 통통 튀다 **바닥에 멎는 그 순간**에만 박자가 맞은 것으로
    친다: 충분히 내려갔고(`_yoff >= BEAT_AT`) 그 자리에서 거의 멎어 있을 때
    (`|_vy| <= BEAT_V`). **한 주기(약 13프레임)에 3프레임(≈0.14초)뿐**이다 — 아무 때나 쳐도
    되면 도전하는 맛이 없다. `BEAT_WINDOW`(20프레임) 안에 이어 맞혀야 하고, 늦으면 끊긴다.
    · 단마다 **다른 재주**(`TRICKS`/`_begin_trick`): 1단 반짝이 · 2단 앞구르기 · 3단 두 바퀴 ·
      4단 팽이돌기(가로 회전) · 5단 둘 다 + 폭죽. 재주 도는 동안 반짝이 꼬리를 흘린다.
      `syk`/`sxk` 를 음수까지 돌리면 도트를 그대로 뒤집어 그리므로 획이 안 뭉개진다.
    · ★ **콤보의 상은 높이가 아니다** — 연타로 자꾸 창 밖에 나가면 화면에 없는 시간이 길어져
      굼떠 보인다. 창 밖은 꾹 누르기의 특전으로 두고, 콤보는 재주·반짝이로 갚는다.
    · ★ **박자를 맞히면 덜 지친다**(`_clicks` +1.0 vs +2.4). 그래야 "너무 빠르게 마구 누르면
      콤보가 아니라 기절에 먼저 닿는다" 가 성립한다 — 박자대로면 식는 속도를 못 이겨 안 뻗고,
      마구 누르면 2초 안에 뻗는다(시뮬레이션으로 확인).
    아무 때나 치면 콤보가 끊기고 **발밑 먼지**만 인다. 리듬이 대략 9프레임(0.4초)이라
    손으로 맞힐 만하다.
  - **반짝이(`_sparks`)는 자리가 두 가지다.** 박자를 맞히면 **머리 위로** 터지고, 아무 때나
    친 것은 **발밑에 먼지**(`foot=True`)만 인다 — 같은 자리에서 크기만 다르면 둘이 구별이
    안 돼 아예 나오는 곳을 갈랐다. 그 밖엔 기절에서 깨어날 때·잔동작(반짝이·폴짝)뿐.
    `_burst(n, power)` 가 뿌리고 `SPARK_COOL` 만큼은 다시 안 뿜으며, `_step_sparks` 가
    굴린다(**기절 중에도 흘러야 하므로 `_idle_step` 이 아니라 `_step_physics` 에서** 부른다).
    ★★ **누를 때마다 뿜지 말 것** — 연타에서 앞것과 겹쳐 지저분하다(실제로 그랬다).
      폰(`Mascot.release`)도 같은 규칙이다 — 기를 채웠거나 콤보 3 이상일 때만.
    ★★ **부채꼴로 가지런히 퍼뜨리지 말 것** — 도형을 그린 것처럼 보인다(실제로 그랬다).
      머리 둘레에 **흩뿌려** 놓고 위로 떠오르며 **수명대로 작아지게** 한다. 폰
      (`Mascot.burst`/`drawSparks`)도 같은 결이고, 폰은 난수 대신 sin 위상을 어긋나게 써서 흩는다.
  - **오래 심심하면 딴짓에 열중한다**(`DEEP_IDLE` ≈ 68초 무반응 → `_begin_absorbed`).
    제자리에서 하는 것은 `_act`(`type`·`nap`·`ball`), 자리를 뜨는 것은 `_away`.
    ★ **딴짓 중에는 `_quiet` 를 세지 않는다** — 안 그러면 하나가 끝나자마자 다음 것이
      곧바로 이어져 산만하다. 끝날 때 `_quiet` 를 0 으로 되감아 다음까지 또 뜸을 들인다.
    · **노트북 두드리기**(`type`) — **옆으로 돌아앉아** 왼쪽에 놓인 노트북(`DESK`)을
      `TYPE_BEAT` 마다 손을 바꿔 두드린다.
      ★★ **노트북만은 도트 격자를 쓰지 않는다**(`_draw_laptop`, `LAP_BASE`/`LAP_LID`).
        한 칸이 2px 이라 노트북 자리(16×15px)에 칸이 8×7 밖에 안 나오고, 그 해상도로는
        무슨 모양·크기·색을 해도 판때기나 쐐기였다(작게·크게·정면·3/4·옆모습·회색·코랄·
        호박색 화면까지 다 해 보고 내린 결론). **1px 다각형 둘 — 위가 왼쪽으로 기운 덮개와
        오른쪽으로 뻗는 얇은 받침** 이면 그 자리에서 바로 노트북이 된다.
        마스코트 본체는 도트 그대로다(폰 앱과 표를 공유하므로 격자를 지킨다).
      ★ 노트북+몸을 합친 그림이 **마스코트 자리(구분선~번갈아 칸, 35px) 안에** 들어가야 하므로
        몸을 `TYPE_SHIFT` 만큼 오른쪽으로 물린다. **노트북을 키우려면 이 값도 같이 손볼 것.**
      ★ 팔은 **`TYPE_ARM`(길게 뻗은 세 칸)** 을 쓴다 — 기본 팔(`ARM`)은 한 칸뿐이라 노트북에
        안 닿아 두드리는 걸로 안 보인다. 두 자세를 번갈아 그려 손이 오르내린다.
      ★ 그래서 이때는 **옆으로 돌아앉는다**:
        · 얼굴이 **옆모습**(`EYES["side"]`, 눈 하나) — 두 눈을 두면 '정면인데 물건만 옆에'가 된다
        · 팔도 **가까운 쪽 하나만**(`arms=(n, None)`, `None` 이면 안 그림)
        · 노트북은 `DESK_X`(-8) 만큼 왼쪽, `DESK_Y`(3) 줄부터 — 격자 밖이다
      ★ 노트북은 **기울이지 않는다**(`paint(..., tilt=0.0)`) — 바닥에 놓인 물건이라
        고개를 따라 기울면 통째로 미끄러진 것처럼 보인다.
    · **낮잠**(`nap`) — 눈 감고 팔을 늘어뜨린 채 **크고 느리게** 숨쉬며, `NAP_EVERY` 마다
      z 를 하나씩 띄운다(`_zzz`, `_draw_zzz`).
      ★ **z 를 3×3 으로 그리지 말 것** — `###/.#./###` 는 가운데 칸이 정확히 한가운데라
        대각선이 안 보여 **I(工)로 읽힌다.** 4×4 라야 z 가 된다(실제로 겪고 고쳤다).
    · **공 놀이**(`ball`) — 호박색 공을 던져 올렸다 받는다. 나는 동안 팔을 번쩍 들고
      받는 순간 내린다. ★ 공을 몸 색으로 그리면 **몸에 붙은 혹**처럼 보인다.
    · **자리 비움**(`away`) — 손 한 번 흔들고(`AWAY_LEAD`) 작업표시줄 **아래로 쏙 내려갔다가**
      한참 뒤 올라온다. 캔버스 밖이라 저절로 잘리므로 따로 지울 것이 없다.
      내려가 있는 동안 `_hit` 은 거짓이다(없는 걸 찌를 수는 없다).
      ★ **비운 사이에 누르면 허겁지겁 달려온다** — 올라오는 시간을 `RUSH_BACK`(6프레임)으로
        줄이고(`_rise`), 올라오자마자 **왼쪽 `RUN_DIST` 만큼 떨어진 자리에서 발을 바꿔 가며
        (`LEGS`↔`LEGS_RUN`) 제자리로 달려온다**(`_runx`·`_running`). 눈은 그동안 동그랗다.
        그냥 튀어오르기만 하면 '불려 왔다' 로 안 읽힌다.
    · 위젯을 누르면 `react()` 가 `_quiet`·`_act` 를 지우고 곧바로 돌아온다.
  - `react(x=None, y=None)` 는 `base.Skin` 에 no-op 로 있어 다른 스킨(카드·아크·표)에선 조용히 무시된다.
  - **build 가 다시 불려도(테마 전환) 옛 루프가 겹치지 않게 세대 토큰 `_anim_gen` 으로 은퇴시킨다.**
    스킨 전환으로 캔버스가 사라지면 `_draw_mascot` 의 `tk.TclError` 를 잡아 루프가 스스로 끝난다.
  - 눈은 `self.c.cget("bg")`(현재 바탕색)로 파내 오류 시 붉은 바탕에서도 얼굴이 보인다.
- 스킨 높이는 `base.taskbar_height()` 실측값에 맞춘다 — 고정 픽셀로 쓰지 말 것
  (작은 작업표시줄 40px, 고배율 60px+ 로 달라진다).
- **작업표시줄도 '항상 위' 라서 그냥 topmost 로 두면 가려진다.** 조작할 때마다
  스스로를 올리므로 `raise_above_taskbar()` 를 1.5초마다 다시 부른다.

## 폰 앱 (android/)

갤럭시(One UI) 기준. Kotlin, appcompat·Compose 안 씀(뷰 + XML), minSdk 26 / target 36.
의존성은 딱 둘 — WorkManager(15분 갱신)와 Play 코드 스캐너(QR).

- **그리기는 `GaugeRenderer` 한 곳**이다. 홈 위젯·앱 화면·상태바 아이콘이 전부 이걸 쓰고,
  배경화면은 `wallpaper/WallpaperArt` 가 같은 조각(`drawBar`·`drawArc`)을 빌려 쓴다. 모양은 여기만 고친다.
- **적정선 눈금(5시간·주간)** — `drawBar`/`drawArc` 는 `due: Float?`(0~100)를 받아 그 자리에 눈금을
  긋는다(색은 최대 대비 `p.title` 고정, 데스크탑 위젯과 같은 그림). 값은 `Limit.dueFraction(now, spanMs)`
  가 창이 흐른 비율로 계산하고, 창 길이는 `Limit.FIVE_SPAN_MS`/`WEEK_SPAN_MS`. 판정(여유/빠름)은
  안 낸다(5시간은 몰아쓰기가 정상). 초기화 지난 창은 `settled` 로 `resetAt=null` → 눈금 숨김.
  넣는 곳: `wide`(두 줄) · `small`(worst 링) · 배경화면 `drawBars`·`drawRings`. 상태바 아이콘엔 없다(숫자뿐).
- **홈 위젯은 반투명이다**(`card = false`) — 배경화면이 그대로 비쳐 보이고 그 위에 미터기만
  뜬다. **배경화면을 못 읽는 요즘 폰에서 '내 배경 그대로 + 사용량'을 얻는 길**이 이것이다.
  ★ 아예 투명하게 두지는 않는다: 위젯 글자색은 폰 테마(밝게/어둡게)를 따르는데 **배경화면이
  밝은지 어두운지는 알 수가 없어서**, 밝은 테마 + 어두운 배경화면이면 글자가 통째로 사라진다.
  그래서 바탕색 판을 65% 알파로 깔고(`plate`), 글자에는 바탕색 후광(`halo`)까지 둘렀다.
  앱 화면은 그대로 꽉 찬 판(`card = true`)을 쓴다.
- ★ **위젯 고르는 화면 그림은 따로 만든다** — `previewLayout` 에 실제 위젯 레이아웃
  (`widget.xml`)을 걸면 **빈 칸으로 뜬다**(런타임에 그림을 넣는 빈 ImageView 라서. 실기 확인).
  `layout/widget_preview_wide.xml`·`widget_preview_small.xml` 이 같은 모양을 보통 뷰로 흉내 낸다.
  ★★ 이 판은 **런처가** 그리므로 **`RemoteViews` 가 아는 뷰만** 쓸 수 있다 —
  여백용으로 `<View>` 를 넣었더니 고르는 화면에 `위젯을 로드할 수 없습니다` 가 떴다.
  LinearLayout·FrameLayout·TextView·ImageView 로만 짤 것. 링 게이지는 각도를 박은
  벡터(`drawable/preview_ring.xml`)다. 폰 없이 보기: `build.ps1 test` → `위젯미리보기_*.png`.
- 위젯 설명(`android:description`)은 `widget_desc`(`5시간·주간 사용량`)다 —
  배경화면 설명(`wallpaper_desc`)을 돌려 쓰면 위젯인데 '배경화면'이라고 적힌다.
- ★★ **위젯 그림은 총량이 1MB 를 넘으면 안 된다.** `RemoteViews` 로 못 넘겨
  `updateAppWidget` 이 거부당하고, 그러면 **위젯이 통째로 빈 칸이 된다** —
  4×1 을 4×2 로 늘렸더니 미터기가 사라진 게 이것이었다(예외를 조용히 삼키고 있어 더 안 보였다).
  `BaseWidget.draw` 가 넓이×높이를 `MAX_PIXELS`(23만 점 ≒ 900KB)로 먼저 줄이고,
  거부당하면 `cooldown-widget` 태그로 로그를 남긴다.
- ★ **넓은 위젯은 세로로 늘려도 4×1 비율을 지킨다**(`WideWidget.render` 가 높이를
  `가로×0.30` 으로 자른다). 칸 높이를 그대로 쓰면 4×2 에서 글자·막대가 두 배로 부풀어
  딴 위젯이 된다. 남는 자리는 투명하게 비워 두고 `fitCenter` 가 가운데로 놓는다.
- 색은 `res/values/colors.xml`(밝게) + `values-night/`(어둡게) — 데스크탑 `skins/base.py`
  팔레트를 옮긴 것. 임계값 50/80 도 같다. **새 색은 두 벌 다 채울 것.**
- **화면에 쓰는 시간은 절대 시각**(`Limit.whenText` → "17:32 초기화")이다. 위젯은 15~30분에
  한 번만 갱신되므로 "2시간 07분 후" 라고 적으면 곧 틀린 값이 된다. 상대 시간(`leftText`)은
  매 프레임 다시 그리는 **배경화면에서만** 쓴다.
- **알림의 남은 시간은 크로노미터에 맡긴다**(`setUsesChronometer`+`setChronometerCountDown`).
  시스템이 1초마다 줄여 주므로 우리가 주기적으로 깨어날 필요가 없다.
- **AOD·상태바 칩은 안드로이드 16 라이브 업데이트**로 간다 — `Notification.ProgressStyle` +
  `setShortCriticalText` 를 단 상시 알림이면 시스템이 알아서 승격한다(One UI 8 Now Bar 도 이 길).
  **API 36 에는 `requestPromotedOngoing()` 이 없다** — 그 뒤 버전에만 있어 리플렉션으로 부른다.
  36 미만에서는 자동으로 옛 진행바 + 숫자 아이콘으로 내려간다.
- **안드로이드 16 미만에서도 AOD 에 숫자가 뜬다** — S20 Ultra(Android 13 / One UI 5.1) 실측:
  라이브 업데이트가 없어도 삼성 AOD 의 알림 아이콘 줄에 우리 비트맵 아이콘이 그대로 나온다.
  16 이상에서 더해지는 건 '확장 진행바'지 '숫자가 처음 보이는 것'이 아니다. 문구를 쓸 때 헷갈리지 말 것.
- **밝은 색을 상태바에 칠했으면 `windowLightStatusBar` 를 켜야 한다.** 안 켜면 시스템 시계·배터리가
  흰 글씨 그대로라 흰 바탕에서 통째로 안 보인다(values / values-night 두 벌 다 둔다).
- 상태바 아이콘은 `GaugeRenderer.statusIcon` 이 **흰 글자 + 투명 배경**으로 그린다.
  시스템이 알파를 마스크로 써서 한 색으로 물들이므로 색을 넣어도 소용없다.
- 포그라운드 서비스는 **쓰지 않는다.** 상시 알림은 그것 없이도 남고, 퍼센트는 클로드를
  쓸 때만 변해서 15분 주기로 충분하다. 정확한 알람 권한도 안 쓴다(`setAndAllowWhileIdle`).
- 배경화면은 **보일 때만** 그린다(`onVisibilityChanged`). 기본 자리는 게이지가 **아래 1/3** —
  잠금화면 시계·알림이 위쪽을 쓴다. (자리는 이제 사용자가 옮길 수 있다. 아래 절)

### 화면 셋

**홈은 지금 값만** 보여 준다(게이지 · 상태 줄 · 새로고침 · 옵션). 손댈 것은 전부
**옵션**(`SettingsActivity`: PC 연결 · 알림 · 배경화면)으로 갔고, 배경화면 세부는
그 아래 **꾸미기**(`CustomizeActivity`)다.

- ★ **PC 연결이 안 돼 있으면 그것부터 하게 만든다** — 홈에서 게이지·새로고침이 사라지고
  `[PC 연결하기]` 가 그 자리에 서며, 앱을 켤 때 **한 번은** 옵션으로 곧장 보낸다
  (`sentToPair` 플래그. 안 두면 옵션에서 뒤로 나올 때마다 다시 튕겨 들어간다).
- **'어디에 넣을지'는 두 갈래다** — `배경화면에 넣기`(→ 꾸미기 화면) / `홈 화면에 위젯 넣기`
  (→ `AppWidgetManager.requestPinAppWidget`, 런처가 '추가할까요?' 를 띄운다).
  ★ 예전엔 `배경화면으로 설정` 과 `배경화면 꾸미기` 가 나란히 있어 **둘이 뭐가 다른지 알 수
  없었다** — 배경화면 쪽은 꾸미기 화면 하나로 합쳤다(거기서 보고 저장하면 그때 걸린다).
  되살리지 말 것.
- 옵션 맨 위 칸 이름이 곧 상태다 — `PC 연결됨` / `PC 연결 — 먼저 하세요`.

### 배경화면 꾸미기

배경은 **쓰던 배경화면(떠 온 것) 또는 고른 사진** 한 갈래다 — 그 위에 미터기를 얹는다.
미터기 모양(막대 / 링 / 숫자만 / 없음)을 고르고 **자리는 미리보기를 손가락으로 끌어서** 정한다.
옵션 > `배경화면에 넣기`.

> 2026-08-02 에 **상어 그림을 저장소에서 뺐다** — 갤럭시 테마 원본이라 공개 저장소·APK 로
> 같이 배포할 것이 아니었다. 대신 **별도 앱**(`com.kyijgnes.sharkwallpaper`, 비공개.
> `13. 클로드 사용량/상어 배경화면/`)이 그림을 갖고, **깔려 있으면 우리가 읽어 와 그린다**
> (`wallpaper/SharkPack.kt`). 안 깔려 있으면 '상어' 칸이 아예 안 뜬다.
>
> ★ **빼는 것은 '그림'뿐이다 — 인식·연동 코드는 지우지 말 것.** v0.9 에서 연동까지 통째로
> 걷어냈다가 상어가 아예 안 떠서 v0.10 에서 되살렸다. 특히 **`AndroidManifest` 의
> `<queries>`(상어 앱 provider)** 가 없으면 안드로이드 11+ 패키지 가시성 때문에
> **깔려 있는지조차 못 본다**(`SharkPack.installed()` 가 늘 false → 칸이 안 뜬다).
> 공개 배포에 문제가 되는 건 그림이고, 그림은 어차피 이 저장소에 없다.
>
> ★★ **라이브 배경화면은 폰에 하나만 걸린다. 그리고 남의 라이브 배경화면은 못 읽는다** —
> `getDrawable()` 로 떠 오는 건 **정적** 배경화면뿐이다. 그래서 '상어 앱을 배경화면으로 걸고
> 그 위에 우리 미터기'는 **원리적으로 불가능**하고, 우리가 상어를 그리는 이 길밖에 없다.
> (그림만 그 앱에서 받아 오므로 공개 APK 에는 남의 그림이 안 들어간다)

> ★★ **'쓰던 배경화면'을 읽으려면 '모든 파일 접근' 권한이 있어야 한다.**
> 없으면 아래 ★★ 대로 막힌다. 꾸미기 화면의 **`쓰던 배경화면 가져오기`** 버튼이
> 시스템 설정 화면(`ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION`)으로 보내고,
> 돌아오면 `onResume` 이 한 장 떠 온다(2026-08-02 실기 확인 — 2400×2400 을 떠 왔다).
> **권한이 없어도 앱은 그냥 돈다** — 그 경우 사진을 직접 고르거나 밋밋한 바탕색이다.
> ★ `Environment.isExternalStorageManager()` 는 저장소가 없는 환경(테스트)에서 던지므로 감싼다.
> ★ **보여 줄 배경이 없는 채로는 배경화면을 걸지 않는다**(`apply` 가 먼저 가져오기로 보낸다) —
> 그대로 걸면 쓰던 배경화면이 밋밋한 바탕색으로 바뀌어 버린다.
>
> ★★ **안드로이드 13 부터 다른 앱은 '지금 배경화면'을 못 읽는다.**
> `WallpaperManager.getDrawable()` 이 `SecurityException: READ_EXTERNAL_STORAGE denied` 로 막힌다
> (targetSdk 33+ 에서는 그 권한을 받을 수도 없다. '모든 파일 접근'이 있어야 뚫린다).
> **실기 확인 2026-08-02, SM-G988N / Android 13.** 그래서 '쓰던 배경화면을 자동으로 떠서
> 그 위에 미터기'는 요즘 폰에서 안 된다 — 사용자가 **사진을 한 번 고르거나**,
> 라이브 배경화면 대신 **홈 위젯**(아래)을 쓰는 길로 간다.
> 옛 기기·권한이 있는 기기에서는 그대로 떠 오므로 코드는 남겨 뒀다(조용히 실패).

- **값은 `Look.Values` 한 덩어리**다(`Look.kt`). `WallpaperArt` 는 **설정을 직접 안 읽고**
  `Values` 를 인자로 받아 그리기만 한다 — 꾸미기 화면이 **저장 전 값**으로 미리보기를
  그려야 하기 때문. `CustomizeActivity` 는 `draft` 만 고치고 `저장` 에서 `Look.write` 로 한 번에 쓴다.
  ★ **낱개 setter 를 되살리지 말 것** — 그 순간 '저장 전'이 없어진다.
- 저장 버튼 **글씨가 곧 상태**다: 아직 우리 배경화면이 안 걸려 있으면 `배경화면으로 걸기`
  (손댄 게 없어도 눌린다), 걸려 있으면 손댄 게 있을 때 `저장` · 없으면 `저장됨`(못 누름).
  ★ 안 걸린 상태에서도 `저장됨` 으로 잠가 두면 **기본 모양이 마음에 든 사람은 배경화면을
  걸 길이 없다** — 실제로 막혔던 자리다.
- **폰에 걸려 있던 배경화면 떠 두기**(`WallpaperGrab`, `filesDir/phone_wallpaper.png`) —
  앱을 열 때마다 다시 뜬다(사용자가 배경을 바꾸면 따라가야 하니까). 어디서 뜨는지는 둘:
  · 우리 게 **안 걸려 있으면** `getDrawable()` — 지금 배경화면 그대로.
  · 우리 게 **걸려 있으면** '지금 배경화면'은 우리라서 `getWallpaperFile(FLAG_LOCK)` —
    **잠금화면 쪽**을 본다. 홈에만 걸어 둔 사람은 거기 자기 그림이 남아 있어서,
    껐다 켜지 않고도 배경을 갱신할 수 있다. 둘 다 없으면 떠 둔 걸 그대로 쓴다.
  주소(파일 이름)는 늘 같으므로 새로 뜬 뒤에는 `WallpaperArt.forgetPhoto()` 로 그림 캐시를 비운다.
  못 떠 오면(위 ★★) 조용히 넘어가고, 못 읽은 까닭은 `cooldown-wallpaper` 태그로 로그에만 남는다.
- ★ **'쓰던 배경'이라는 칸 이름은 떠 온 그림이 실제로 있을 때만 쓴다.** 없는데 그렇게 적으면
  이름만 '쓰던 배경'이고 화면엔 아무 그림도 없는 거짓말이 된다 — 그 경우 버튼은 `사진 고르기`.
  직접 고른 사진을 쓰는 중이면 `다른 사진` + `쓰던 배경으로` 두 개가 선다(떠 온 그림이 있을 때만).
- **설명 대신 미리보기다.** 고르면 그 자리에서 바뀌고, 크기 % 는 칸 이름에 그대로 적는다
  (`미터기 크기 · 120%`). 툴팁·도움말 문단은 넣지 않는다(폴더 전체 규칙).
- **끌면 짚은 것이 따라온다** — 미터기를 짚으면 미터기가(`hitsMeter`), 그 밖을 짚으면 배경이.
  끌기 계산은 `WallpaperArt.dragMeter`/`dragBg` 안에만 둔다(화면 쪽은 픽셀 차이만 넘긴다).
- ★ **자리는 언제나 '이미 화면 안으로 가둬진 자리'에서 시작해 옮긴다**(`meterRect` 가 가둔다).
  날값을 계속 더하면 화면 밖으로 나간 만큼 되돌릴 때 먹통 구간이 생긴다.
- ★ **미터기 판은 화면 너비를 못 넘는다** — 글자 크기가 판 너비에서 나오므로 넘으면
  글자가 통째로 잘린다(`METER_MAX` 1.15 + `meterRect` 에서 한 번 더 자른다).
- ★★ **떠 온 배경화면은 주소만으로 캐시하면 안 된다.** 늘 같은 파일 이름
  (`phone_wallpaper.png`)에 덮어쓰므로 배경을 바꿔도 주소가 그대로다 — 그러면
  **배경화면을 그리는 프로세스가 옛 그림을 계속 쓴다**(실제로 '배경을 바꿔도 옛 그림'
  으로 나왔다). 캐시 키에 **파일이 마지막으로 바뀐 때**를 섞는다(`stampOf`).
  `forgetPhoto()` 는 부른 프로세스의 캐시만 비우므로 그것만으로는 안 된다.
- ★ **떠 오기는 앱을 열 때마다**(`onResume`) 한다. `onCreate` 에서만 하면 앱이 뒤에
  살아 있을 때 배경을 바꿔도 안 따라온다. 손으로 시키는 손잡이도 뒀다 —
  꾸미기 화면의 `지금 배경화면으로 갱신`.
- **사진 배경**: `ACTION_OPEN_DOCUMENT` + `takePersistableUriPermission`(안 잡으면 재부팅 후
  사진이 사라진다). 읽기는 `WallpaperArt` 가 **주소+바뀐때+화면크기 키로 캐시**한다 — 16fps 로
  매 프레임 디코딩하면 폰이 뜨거워진다. 폰 사진은 4000×3000(48MB)이 흔해서
  **화면 넓이의 3배로 줄여 읽는다.** 못 읽으면(안 골랐거나 지웠거나) **밋밋한 바탕색**으로 둔다.
- 사진 위에서는 글씨가 안 보이므로 **`글씨 뒤 판`이 사진일 때 기본으로 켜진다**
  (`Look.Values.plateOn` 의 '안 정했으면' 규칙). 사용자가 끄면 그 선택이 이긴다.
- 확인: `build.ps1 test` → `app/build/미리보기/` 의 `꾸미기_*` 와 **`꾸미기화면.png`**

### 배경화면에 사는 클로디

미터기 칸의 `클로디` 토글로 켜고 끈다. 자리는 미리보기에서 끌어 옮긴다. **놀 수 있다:**

- **콕** 찌르면 펄쩍 뛰며 눈이 커진다. **길게 누르면** 쭈그려 앉아 떨며 **기를 모으고**,
  떼면 모은 만큼 높이 뛴다. **연타하면 기절**한다(X_X + 별).
- **반짝이는 크게 뛸 때만** 나온다(`burst(count, power)`) — `(기 0.6 이상) 또는 (연타 콤보
  3 이상)` 일 때 `SPARK_BIG`(+콤보), 그리고 기절에서 깨어날 때 `SPARK_WAKE`.
  ★ 이래서 "가끔만 반짝인다" 로 보인다 — **그게 맞다.** 타이밍을 맞혀야 나오는 상이다.
    뗄 때마다 나오게 바꿔 봤다가 **연타에서 앞것과 겹쳐 지저분해 되돌렸다**(데스크탑도
    같은 규칙: `slim.py` 는 튀는 속도가 `SPARK_AT` 을 넘을 때만).
  ★ 홈 화면에서는 길게 누르기를 런처가 채 가므로, 거기서 반짝이를 보는 길은 **연타 콤보**뿐이다.
  모양은 데스크탑 `_burst` 와 같은 결(머리 둘레에 흩뿌려 떠오르며 수명대로 작아짐).
- 그림표는 `MascotSprite.kt` — **앱 아이콘과 같은 생성기**(`android/art/make_claudi_icon.py`)가
  데스크탑 `pc/windows/skins/slim.py` 에서 뽑는다. **모양은 slim.py 만 고치고 스크립트를 돌린다.**
- 움직임은 `Mascot.kt` 하나에 모았다. **상태를 들고 있으므로 그리는 쪽마다 하나씩** 만든다
  (배경화면 엔진 하나, 꾸미기 미리보기 하나). `WallpaperArt` 는 상태를 안 갖는다.
- ★ **기울임은 회전이 아니라 계단식 밀기**다(윗줄일수록 옆으로 더). 도형을 돌리면 도트가 뭉개진다.
- ★★ **용수철은 픽셀이 아니라 '칸' 단위로 센다.** 폰은 도트도 화면도 커서 데스크탑과 같은
  픽셀 값을 쓰면 제자리 꿈틀거림밖에 안 된다 — 칸으로 세야 어느 화면에서나 **제 몸 높이의
  몇 배**로 똑같이 튄다(`LIFT`·`JUMP` 전부 칸).
- ★★ **칸 경계를 정수 픽셀로 반올림해서 그린다.** 실수 좌표로 그리면 이웃 칸이 서로 다르게
  반올림돼 **머리카락 같은 틈**이 줄줄이 생긴다(폰에서 실제로 그랬다). 좌우·상하 모두 같은
  식을 반올림하므로 이웃 칸이 정확히 같은 선을 쓴다.
- **미터기 판 위에 그린다** — 기본 자리가 판 안쪽(제목 줄 옆 빈자리)이라 뒤로 가면 가려진다.
- 크기는 `mascotCell` = 화면 너비 ÷ 95. 자리는 `mascotX/Y`(비율), 미리보기에서 끌어 옮긴다.
- ★ **길게 누르기는 홈 화면에서 런처가 채 간다**(위젯·배경화면 메뉴가 뜬다) — 기 모으기는
  꾸미기 미리보기에서만 제대로 된다. 홈에서는 **연타 콤보**(빠르게 이어 찌르면 점점 높이)가
  그 자리를 맡는다. 실기 확인.
- ★ **알림을 내리면 큰 아이콘 자리에 클로디가 보인다**(`GaugeRenderer.mascotIcon`).
  상태바 작은 아이콘은 시스템이 알파만 보고 한 색으로 물들여 숫자밖에 못 넣지만,
  **큰 아이콘은 색 그대로** 나온다.
- ★ 터치는 `Engine.setTouchEventsEnabled(true)` + `onTouchEvent` 로 받는다 —
  **빈 곳을 눌렀을 때만 온다**(아이콘·위젯 위는 그쪽이 먹는다). 실기 확인.
  ★ 런처가 길게 누르기를 **자기 메뉴로 채 가면 UP 이 안 온다** — 화면이 가려질 때 물린다.
  (꾸미기 화면 자체도 폰 없이 렌더한다. 미리보기 크기가 화면 크기에서 나오므로
  그 테스트에는 `qualifiers = "w360dp-h780dp-xxhdpi"` 를 붙여 **진짜 폰 해상도로 재야** 뜻이 있다).

### 앱 아이콘은 클로디 (`res/drawable/ic_claudi.xml`)

데스크탑 위젯 슬림 바의 코랄 **도트 마스코트**를 폰으로 옮겼다 — PC·폰이 한 앱으로 읽힌다.

- ★ **이 xml 은 손으로 고치지 않는다.** `android/art/make_claudi_icon.py` 가
  **`pc/windows/skins/slim.py` 의 도트 표(HEAD/LEGS/ARM/EYES)를 그대로 읽어** 만든다.
  (2026-08-03: PC 파일이 `pc/` 로 옮겨진 뒤 이 스크립트의 `sys.path` 가 옛 경로를 가리켜
   `ModuleNotFoundError` 로 죽고 있었다 — 고쳤다. 표를 고쳤으면 **꼭 다시 돌려 볼 것.**)
  마스코트를 바꿀 땐 **slim.py 표만 고치고 스크립트를 다시 돌린다** — 그래야 PC·폰이
  안 어긋난다. (옛 별빛 아이콘은 손으로 그렸다가 데스크탑을 고칠 때마다 따로 놀았다)
- ★ **눈은 흰 도형이 아니라 `evenOdd` 로 뚫은 구멍**이다. 그래야 단색(monochrome)
  아이콘에서도 얼굴이 남는다(흰색으로 칠하면 통째로 물들어 사라진다).
- ★ **팔 끝까지 지름 66 원 안**에 들어가야 한다(스크립트의 `CELL` 주석에 계산식).
  원으로 잘리는 런처에서 팔·다리가 잘리면 안 된다.
- 확인: `build.ps1 test` → `앱아이콘_432.png` · `앱아이콘_144.png`
- **실기 확인됨**(2026-08-02, SM-G988N): 삼성 스퀘어클 마스크에서 팔·다리 안 잘리고
  눈도 또렷하다. 상태바에서도 `⁝4⁝`(네모 점 넷 + 숫자)로 읽힌다.

### 알림은 스위치 하나 (셋을 못 가른다)

`상태바 · 잠금화면 · AOD` 는 **한 알림 하나**라서 따로 켜고 끌 수 없다. 셋으로 갈라 봤다가
되돌렸다 — 실기(SM-G988N)에서 이렇게 나왔다:

- 상태바 아이콘을 없애는 유일한 손잡이는 채널 중요도 **`IMPORTANCE_MIN`** 인데,
  그걸로 내리면 **삼성 잠금화면·AOD 에서도 통째로 사라진다.** 상태바만 끄기는 불가능.
- `VISIBILITY_SECRET` 은 잠금화면을 감추지만 AOD 가 딸려 간다.
- 그래서 스위치는 하나다. ★ **`IMPORTANCE_MIN` 으로 내리지 말 것.**
- 그때 잠깐 만든 `cooldown_quiet` 채널은 `update()` 가 지우고 다닌다(안 지우면 폰 설정에 남는다).

### 상태바·AOD 에서 잘 보이게

- ★ **`Engine.onComputeColors()` 가 핵심이다.** 이걸 안 주면 시스템이 배경 밝기를 모르고
  상태바 시계·아이콘을 **흰 글씨로 남겨 둬서**, 밝은 바다 위에서 통째로 안 보인다
  (우리 숫자 아이콘까지 같이 사라진다 — 실제로 그랬다). 화면을 96×211 로 작게 한 번 그려
  `WallpaperColors.fromBitmap` 에 넘기면 **밝기 판단은 시스템이 알아서** 하고, 사진 배경까지
  저절로 맞는다. `windowLightStatusBar` 는 앱 화면 얘기라 배경화면에는 아무 상관이 없다.
- ★ **상태바 숫자는 '두 자리(`00`)' 기준으로 크기를 정한다**(`GaugeRenderer.statusIcon`).
  그리는 글자로 재면 7% 일 때만 칸을 꽉 채워 커졌다가 10% 가 되면 확 작아진다 —
  그 들썩임이 작은 것보다 더 눈에 띈다. `–`(값 없음)도 잉크가 납작해서 기준으로 삼으면
  화면 폭짜리 막대가 된다. 세 자리(`100`)일 때만 폭에 맞춰 한 번 더 줄인다.
  글꼴은 좁은 볼드(`sans-serif-condensed` BOLD) — 세 자리도 안 가늘어진다.
- **숫자를 네모 점 넷이 감싼다** — 앱 아이콘(도트 마스코트)과 같은 결이라 상태바에서도
  우리 것인 줄 안다. ★ **네 모서리에 점으로만** 놓는다. 처음에 대각선 짧은 '살'로 그렸더니
  24dp 에서 숫자 획과 섞여 `17` 이 `)17(` 처럼 읽혔다 — 실기에서 확인하고 점으로 바꿨다.
  점은 획으로 안 보여서 숫자를 안 건드린다. 가로·세로 살은 애초에 못 넣는다 —
  그 자리를 숫자가 다 쓰고, 숫자를 줄이면 그게 더 안 보인다.
- ★ **AOD 에서 쓸 수 있는 자리는 이 아이콘 하나뿐이다.** 라이브 배경화면은 AOD 에 아예
  안 그려지고(AOD 는 별도 화면 — `DozeWallpaper` 없음 실측), 진행바 승격은 안드로이드 16 부터다.
- **아이콘에 게이지 막대를 넣어 봤다가 뺐다** — 되긴 됐지만(빈 칸을 옅은 알파로 두면 물들여진
  뒤에도 진하기 차이가 남는다) 숫자만 작아지고 실효가 없었다. 되살릴 거면 그 알파 요령만 기억할 것.

### 빌드

- **`android/build.ps1`** 로 돌린다 (`build` / `install` / `install-release` / `test` / `release`).
- ★ **한글 경로는 유닛 테스트만 깨뜨린다.** 저장소가 `13. 클로드 사용량` 아래라, Gradle 이
  테스트용 자식 JVM 에 클래스패스를 넘길 때 한글이 깨져 `ClassNotFoundException` 이 난다.
  **APK 빌드는 한글 경로에서도 된다.** 그래서 `build.ps1` 은 `test` 일 때만
  `%LOCALAPPDATA%\cooldown-android` 정션을 거치고, 나머지는 저장소에서 바로 돌린다 —
  정션이 망가져도 APK 빌드는 안 막힌다. 손으로 `gradlew` 를 부르지 말 것.
  같은 이유로 `gradle.properties` 에 `android.overridePathCheck=true` 가 필요하다.
- ★ **정션은 읽기는 되면서 쓰기만 조용히 사라지는 상태가 된다** (원인 미확정 — 보안 솔루션 의심).
  그러면 Gradle 이 "Failed to create directory" 로 죽는다. `Test-Path` 도 참이고
  파일 목록도 제대로 보이므로 그런 검사로는 못 잡는다 — `build.ps1` 은 **정션으로 쓴 폴더가
  저장소에 실제로 나타나는지**로 판정하고(`Test-JunctionWrites`), 아니면 지웠다가 다시 만든다.
- **`gradlew testDebugUnitTest` 는 폰 없이 화면 그림을 뽑는 도구다** —
  `app/build/미리보기/*.png` 에 위젯·배경화면·상태바 아이콘이 최악값까지 렌더된다.
  화면을 고쳤으면 이걸로 먼저 확인할 것 (데스크탑의 `_shot_skin.py` 와 같은 역할).
- 폰에 넣기: `.\build.ps1 install-release` (USB 나 무선 디버깅으로 연결).
  **`install`(디버그 키) 대신 `install-release` 를 쓸 것** — 디버그 키로 깔아 두면 나중에
  나눠주는 APK 로 덮어쓸 수가 없다(서명이 달라 '앱이 설치되지 않음').

### 나눠주기 (친구에게 APK)

**태그를 밀면 GitHub Actions 가 APK 를 만들어 릴리스에 붙인다** (`v0.2` 처럼). 순서:

1. `app/build.gradle.kts` 의 **`versionCode` 를 올린다.** 안 올리면 폰이 업데이트인지 못 가린다.
2. `git tag v0.N` → `git push origin v0.N`

- ★ **서명은 고정 키다.** 예전엔 release 도 디버그 키로 서명했는데, 디버그 키는 PC 마다 다르고
  CI 는 매번 새 VM 이라 **빌드마다 서명이 바뀌어 친구가 업데이트를 못 받았다**(지웠다 깔아야 하고
  그러면 페어링 QR 도 다시). 지금은 `android/keystore/cooldown-release.jks` 로 서명한다.
  지문 `SHA-256 726dfa27…7c117b` / `CN=Claude Cooldown, O=kyijgnes, C=KR`.
- 키와 비밀번호는 **`android/keystore.properties`(gitignore)** 에 있고, CI 는 시크릿
  **`COOLDOWN_KEYSTORE_B64`**(jks 를 base64) · **`COOLDOWN_KEYSTORE_PASSWORD`** 로 받는다.
  **이 시크릿이 키의 백업이다** — 로컬 파일을 잃으면 여기서 되살린다.
- ★ **키를 잃으면 끝이다.** 친구들이 이미 깐 앱을 다시는 업데이트 못 한다(전부 지우고 재설치).
  **새 키를 만들지 말고** 시크릿에서 복구할 것.
- 시크릿이 없으면 워크플로가 **일부러 첫 단계에서 멈춘다** — 조용히 디버그 서명으로
  나가면 이 문제가 되돌아오기 때문. 서명 지문은 빌드 로그('서명 지문 남기기')에 남는다.
- 손으로 만들 때: `.\build.ps1 release` → `app/build/outputs/apk/release/app-release.apk` (3.1MB)

## exe 로 묶을 때 (pc/build_exe.py)

- 빌드는 **`pc/exe 빌드.bat` 더블클릭** 한 번이면 된다(PyInstaller 자동 설치 → `pc/build_exe.py` 실행).
  결과 `pc/dist/클로드 쿨다운.exe` 하나만 배포하면 된다. 콘솔에서 직접: `python pc/build_exe.py`.
- `skins/__init__.py` 가 `importlib` 로 스킨을 불러오므로 PyInstaller 가 스스로
  못 찾는다. **`--hidden-import skins.card` 처럼 스킨마다 넣어야 한다.**
  빼먹으면 exe 에 디자인이 하나도 안 들어간다.
- `pc/cooldown_core.py`·`pc/cooldown_ping.py`·`pc/cooldown_push.py` 가 `pc/`(build_exe 와 같은 곳)에 있어
  `--paths` 로 `pc/`(ROOT)와 `pc/windows` 를 둘 다 준다(+ `HIDDEN` 에 명시). 공용 모듈을 새로 추가하면 `HIDDEN` 에도 넣을 것.
- **버전은 손으로 붙인다.** 빌드 결과는 늘 `pc/dist/클로드 쿨다운.exe` 라, 만든 뒤
  `클로드 쿨다운 v0.0N.exe` 로 이름을 올려 두고(옛 판은 남겨 둔다) **그 exe 를 한 번 실행**한다 —
  `repair_autostart()` 가 자동 실행 바로가기를 자기 자신으로 갈아끼운다. 먼저 옛 판을 끝내야 한다
  (뮤텍스 때문에 두 번째 프로세스는 창을 안 띄우고 그냥 끝난다).
- ★ **기능을 더했으면 exe 를 다시 만들 것.** 자동 실행은 `dist/` 의 exe 를 가리키므로,
  안 다시 만들면 **로그인할 때마다 옛 exe 가 떠서 새 기능이 통째로 빠진 채로 돈다.**
  2026-07-29 에 실제로 겪었다 — 위젯은 조회·표시가 멀쩡한데 폰으로만 안 보내고 있었고,
  폰에는 그냥 옛 값이 떠 있어 원인을 찾기까지 한참 걸렸다. 증상이 '폰만 안 맞는다' 면
  **`~/.claude_cooldown_push.json` 의 `last_ok` 가 도는지부터** 본다.
- 자동 실행 바로가기는 `launch_command()` 가 판단한다 — 묶인 상태(`sys.frozen`)면
  exe 자신을, 아니면 pythonw + 스크립트를 등록한다. 옮겨도 시작할 때 스스로 고친다.
- 서명이 없어 처음 실행할 때 SmartScreen 이 막는다. 정상이며 `추가 정보 > 실행`.

## 다음 작업

1. ~~폰 앱 화면·배경화면 만들기~~ **완료.** 파란 테마(`colors.xml` 두 벌)에 라이브
   배경화면·홈 위젯·상태바 아이콘까지 붙었고 실기(SM-G988N)에서 확인했다.
   **게이지 초록/노랑/빨강은 의미(여유·보통·임박)라 그대로 뒀다** — 파랑은 배경·아이콘에서만.
   확인은 `build.ps1 test` → `app/build/미리보기/`.
2. ~~쓰던 배경화면 + 미터기~~ **완료(길을 바꿔서).** 안드로이드 13 부터 배경화면을 못 읽어
   라이브 배경화면으로는 불가능하다 — **반투명 홈 위젯**이 그 자리를 맡는다(위 '폰 앱' 절).
3. **작업표시줄용 슬림 바 다듬기** — 크기·오른쪽 번갈아 칸까지 됐다. 남은 건 취향 정도.
4. `limits[]` 의 `severity` 를 색상 임계값 대신 쓸지 검토 (지금은 50/80 자체 기준)
5. 폰 아이콘·상태바 장식을 데스크탑 클로디와 계속 같이 가게 두기
   (`android/art/make_claudi_icon.py` 를 다시 돌리면 된다)

## 하지 말 것

- ★★ **릴리스 노트를 쓰지 말 것.** 릴리스에는 **제목(태그)과 받을 파일(exe·apk)만** 둔다 —
  본문은 **비운다.** 변경 내용을 적지 말고, `gh release create/edit` 의 `--notes`·`-n`·
  `--notes-file`·`--generate-notes` 를 쓰지 말 것. 워크플로도 마찬가지
  (`.github/workflows/android.yml` 에 `body: ""` · `generate_release_notes: false` 로 박아 뒀다).
  받는 사람이 볼 것은 파일이지 설명이 아니다. 설명이 필요하면 README 한 곳에만.
  - ★★ **태그는 반드시 `git tag -a v0.N -m ""`(annotated·빈 메시지)로 만들 것.**
    그냥 `git tag v0.N`(lightweight)로 만들면 **본문이 비어 있어도 GitHub 이 그 자리에
    커밋 메시지를 통째로 띄운다** — 실제로 v0.10 에서 커밋 메시지가 `Co-Authored-By` 까지
    릴리스 페이지에 노출됐다(본문에 공백을 넣어도 안 없어진다. 태그를 annotated 로
    바꿔야 그 블록이 사라진다). 확인: `git cat-file -t $(git rev-parse v0.N)` 가 `tag` 여야 한다
    (`commit` 이면 lightweight).
- `.credentials.json` 내용이나 accessToken 을 서버로 보내지 말 것. 퍼센트만 전송.
- **`android/keystore/` · `android/keystore.properties` 를 커밋하지 말 것.** 서명 키와 비밀번호다.
  `.gitignore` 에 있지만 `git add -f` 로 뚫지 말 것.
- **폰이 `api.anthropic.com` 을 직접 치게 만들지 말 것.** OAuth 리프레시가 토큰을
  회전시켜 **PC 의 Claude Code 로그인이 풀린다.** 폰은 릴레이에서만 읽는다.
- `UPSTASH_REDIS_REST_TOKEN` 을 클라이언트 번들이나 에이전트에 넣지 말 것.
- 폴링 간격을 300초 미만으로 낮추지 말 것 (폰은 15분).
- **릴레이의 주소·경로·GET 응답 일곱 필드를 바꾸지 말 것** — 폰에 이미 깔린 앱이
  전부 재페어링이다. 저장소를 바꿔도 이건 그대로 간다(`server/relay_check.mjs` 로 확인).
- **릴레이 로그·에러 메시지에 `key` 를 남기지 말 것.** 읽기 비밀번호다
  (저장소에도 원문이 아니라 SHA-256 만 들어간다).
- 위젯에서 `os.startfile` 로 외부 파일·프로그램을 열지 말 것 — 프로즌(`--windowed`)·
  pythonw 환경에서 앱이 통째로 꺼지는 일이 있다. 파일 내용은 창을 직접 그려 보여 준다.
- **팝업(핑 시각·핑 기록·사용 속도·폰 연결)은 본체 위젯과 같은 결로 통일** — OS 창틀이 아니라
  `_open_panel(title, click_away=)`(테두리 없는 둥근 창 + 왼쪽 액센트 바 + 볼드 제목 + ✕·Esc·드래그)
  으로 만들고, 내용을 채운 뒤 `_finalize_panel(top, width, grab=)` 로 크기·위치를 잡는다.
  버튼은 `_themed_button`, 색·글꼴은 카드 스킨과 같은 팔레트(`P`)·`KR`/`NUM`·`PANEL_PAD`.
  새 팝업을 만들 때도 이 세 헬퍼를 쓸 것(Toplevel 을 날것으로 쓰지 말 것).
  - **한 번에 하나만 뜬다** — `_open_panel` 이 열 때 먼저 `_close_panels()` 로 옛 팝업을 닫는다.
    **위젯을 누르거나(`_press`) 디자인·밝기를 바꾸면(`switch_skin`·`_apply_theme`) 열린 팝업을 닫는다** —
    안 그러면 옛 색·옛 자리의 팝업이 떠돌아 안 닫히는 것처럼 보인다(실제로 났던 버그).
  - **읽기 전용 팝업은 `click_away=True`(기본)** — 밖(위젯·바탕화면·다른 앱)을 누르면 포커스가
    빠져 저절로 닫힌다(`_panel_focus_out`, `_finalize` 가 `focus_force` 로 포커스를 쥐어 줌).
    **편집 팝업(시각 설정·폰 연결)은 `click_away=False`** — 타이핑 중 사라지면 안 되므로.
- 내가 만드는 `.bat` 은 **순수 ASCII 로 쓸 것.** Write 도구가 UTF-8(BOM 없음)로 저장하는데
  한국어 Windows cmd 는 배치를 cp949 로 읽어, 한글이 든 줄(주석 포함)에서 바이트가 어긋나
  뒤따르는 명령까지 깨진다. 한글 안내는 파이썬(`pc/build_exe.py`)이 콘솔에 출력하게 맡긴다.
- 폰 앱에 **툴팁·도움말 문단을 넣지 말 것**(폴더 전체 규칙). 설명이 필요하면 항목 이름을
  하는 일로 바꾸고 지금 상태를 화면에 그대로 보여 준다 — 예: 알림 스위치 이름이
  `상태바 · 잠금화면 · AOD`(이 스위치가 켜는 것) 자체다.
  2026-08-02 에 남아 있던 안내 글씨를 **전부 걷어냈다**: 알림 스위치 밑 `진행바로 크게 /
  클로디 안에 숫자`, 옵션 맨 아래 `홈 위젯은 홈 화면 길게 누르기 → …`, 꾸미기 화면의
  `끌어서 자리 옮기기`. **되살리지 말 것** — 필요하면 칸 이름·버튼 이름으로 말한다.
