<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ★ **쓰던 배경화면을 그대로 두고 미터기만 얹기 위한 권한.**
         안드로이드 13 부터 `WallpaperManager.getDrawable()` 이 이것 없이는
         SecurityException 으로 막힌다(실기 확인). **없어도 앱은 그냥 돈다** —
         그 경우 배경은 사진을 직접 고르거나 밋밋한 바탕색이다.
         사용자가 옵션에서 눌러야 켜지고(시스템 설정 화면), 우리는 배경화면을
         한 장 떠 오는 것 말고는 아무 파일도 건드리지 않는다. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- 라이브 배경화면을 쓸 수 있는 기기에서만 후보로 뜨게 (필수는 아님) -->
    <uses-feature
        android:name="android.software.live_wallpaper"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- PC 위젯이 띄운 QR 을 폰 기본 카메라로 찍어도 바로 열리게 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="claudecooldown" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- 옵션 — PC 연결·알림·배경화면 -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="옵션"
            android:parentActivityName=".MainActivity" />

        <!-- 배경화면 꾸미기 (미리보기 + 끌어서 자리) -->
        <activity
            android:name=".CustomizeActivity"
            android:exported="false"
            android:label="배경화면 꾸미기"
            android:parentActivityName=".SettingsActivity" />

        <!-- 홈 위젯 -->
        <receiver
            android:name=".widget.SmallWidget"
            android:exported="true"
            android:label="@string/widget_small">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small" />
        </receiver>

        <receiver
            android:name=".widget.WideWidget"
            android:exported="true"
            android:label="@string/widget_wide">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_wide" />
        </receiver>

        <!-- 초기화 시각에 한 번 깨어나 0% 로 바꾼다 -->
        <receiver
            android:name=".work.ResetAlarm$Receiver"
            android:exported="false" />

        <!-- 재부팅 후에도 알림·주기 갱신을 되살린다 -->
        <receiver
            android:name=".work.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 라이브 배경화면 (홈·잠금) -->
        <service
            android:name=".wallpaper.CooldownWallpaperService"
            android:exported="true"
            android:label="@string/wallpaper_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper" />
        </service>
    </application>
</manifest>
