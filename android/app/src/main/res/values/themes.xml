<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- appcompat 없이 간다. 색은 전부 우리 팔레트(@color/*)를 화면에서 직접 지정하므로
         부모 테마의 밝기 종류는 따지지 않는다 — 제목표시줄만 없앤다. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg</item>

        <!-- ★ 상태바를 밝은 색으로 칠했으면 이걸 켜야 시계·배터리 글씨가 **검게** 바뀐다.
             안 켜면 흰 글씨가 흰 바탕에 얹혀 통째로 안 보인다. 어둡게는 values-night 에서 끈다.
             (짝인 windowLightNavigationBar 는 API 27 부터라 minSdk 26 에서 못 쓴다.
              내비게이션 바는 시스템이 알아서 맞춰 주므로 없어도 된다 — 실기 확인) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 홈 위젯에서 알을 깼을 때 뜨는 공룡 점프(`DinoOverlayActivity`) — 바탕이 비어 있어
         홈 화면이 그대로 보이고 판만 위젯 줄에 깔린다. 뜨고 닫힐 때 움직임도 없앤다.
         밝게/어둡게를 안 가른다(칠하는 바탕이 없다). -->
    <style name="DinoOverlay" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
